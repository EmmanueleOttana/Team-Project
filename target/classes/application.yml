spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fakedb
    username: root
    password: Irisviel95
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  jackson:
    serialization:
      fail-on-empty-beans: false